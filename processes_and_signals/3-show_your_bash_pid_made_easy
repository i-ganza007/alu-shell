#!/usr/bin/env bash
# Displaying all top things

# Use pgrep to find processes containing the word "bash" in their name
pgrep -a bash | while read -r pid; do
  # Extract the process name from the output of pgrep
  process_name=$(ps -o comm= -p "$pid")
  echo "PID: $pid, Process Name: $process_name"
done

